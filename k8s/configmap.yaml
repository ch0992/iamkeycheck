# k8s/configmap.yaml
# .env → ConfigMap 변환 결과 예시 (실제 값은 CI/CD에서 주입)
apiVersion: v1
kind: ConfigMap
metadata:
  name: iamkeycheck-config
  namespace: default
data:
  STAGE: "dev"
  CSV_PATH: "/app/secrets/applicant_accessKeys.csv"
  LOG_LEVEL: "INFO"
