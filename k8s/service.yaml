# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: iamkeycheck-service
  namespace: default
spec:
  selector:
    app: iamkeycheck
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
